# Populated automatically by Vercel when you connect a Postgres database
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=

# Shared secret the GitHub Actions workflow must send as
# `Authorization: Bearer <INGEST_API_KEY>` when POSTing results
INGEST_API_KEY=
